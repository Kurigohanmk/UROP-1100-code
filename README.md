# UROP-1100-code
This is the reproduction of the result in the paper 'Complete quantum-state tomography with a local random field' by Python

qutip and numpy are needed for running the code. It can be done by

`pip install qutip`

`pip install numpy`

